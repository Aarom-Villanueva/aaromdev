Elimina el número del badge de la sección de Proyectos/Productos para mantener la consistencia con el resto del sitio:

1. Modificar el Badge en ProjectsSection.tsx:
Busca el componente de cabecera de sección (o etiqueta de categoría) donde se renderiza el texto 03 — PRODUCTOS (o 03 — PROYECTOS).

Cambia el texto del badge para que quede únicamente como PRODUCTOS (o PROYECTOS), conservando el mismo estilo de línea y tipografía en mayúsculas que tienen las demás secciones (CAPACIDADES APLICADAS y PERFIL).

2. Verificación:
Confirma que tsc --noEmit siga pasando sin errores.