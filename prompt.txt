Ejecuta una optimización de espaciado móvil y un fix para la tarjeta VANTA-01:

1. Reducción de espaciado móvil (app/page.tsx, HeroSection.tsx, ProjectsSection.tsx):
Reduce drásticamente el espaciado excesivo entre el final de <HeroSection/> y el inicio de <ProjectsSection/> para que la sección de productos aparezca mucho más arriba en móviles.

En app/page.tsx, reduce cualquier margin-top o gap excesivo entre estos dos componentes.

En HeroSection.tsx, reduce el espaciado inferior del bloque de texto 'DESLIZA PARA DESCUBRIR'.

En ProjectsSection.tsx, reduce el padding superior de la sección.

2. Fix de video de VANTA-01 (data/products.ts, ProjectsSection.tsx):
Aborda el hallazgo sobre la falta de póster real para VANTA-01.

En data/products.ts, proporciona un nuevo póster real para VANTA-01. Usa una imagen de marcador de posición técnica genérica (ej. /screenshots/vanta-placeholder.png). Asumimos que esta imagen existe o puede ser una imagen técnica de stock genérica.

En ProjectsSection.tsx, actualiza la tarjeta VANTA-01 para usar el elemento <video> optimizado con el nuevo póster real, eliminando el 'placeholder oscuro' y asegurando que el video de Cloudinary se cargue y reproduzca en móviles con preload="metadata".

3. Verificación:
Confirma que tsc --noEmit pase sin errores.